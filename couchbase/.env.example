# Couchbase Environment Configuration

# Domain Configuration (for Traefik reverse proxy)
# Set DOMAIN_PREFIX and DOMAIN for domain-based access
# Example: couchbase.yourdomain.com
DOMAIN_PREFIX=couchbase
DOMAIN=example.com

# Localhost Configuration (alternative to domain)
# Uncomment to use localhost access instead
# HOST_PORT=8091

# Couchbase Configuration
COUCHBASE_TAG=community
CLUSTER_NAME=couchbase-cluster
COUCHBASE_ADMINISTRATOR_USERNAME=Administrator
COUCHBASE_ADMINISTRATOR_PASSWORD=SecurePassword123!
COUCHBASE_BUCKET=default
# Admin console will be available at port 8091
# Additional ports: 8092 (Views), 8093 (Query), 8094 (Search), 11210 (Data)

# Network Configuration
DOCKER_NETWORK=shared_network