# Domain Configuration
DOMAIN=example.com
DOMAIN_PREFIX=rabbitmq

# Port Configuration
# Uncomment to expose ports directly (not just through Traefik)
#HOST_PORT=5672
#MANAGEMENT_PORT=15672 

# Docker Configuration
RABBITMQ_IMAGE_TAG=4.2-management-alpine
PIDS_LIMIT=200