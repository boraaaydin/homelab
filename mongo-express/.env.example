# Mongo Express Configuration
MONGO_EXPRESS_TAG=latest
MONGO_EXPRESS_USERNAME=admin
MONGO_EXPRESS_PASSWORD=change_this_password

# MongoDB Connection
MONGO_HOST=mongodb
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=change_this_password

# Domain Configuration (choose one approach)
# Option 1: Domain-based access (requires Traefik)
DOMAIN_PREFIX=mongo-express
DOMAIN=example.com

# Option 2: Localhost access
# HOST_PORT=8081