# SeaweedFS Configuration

# Docker Image Configuration
DOCKER_IMAGE=chrislusf/seaweedfs
DOCKER_TAG=latest

# Domain Configuration
# Master server domain (admin UI and API)
DOMAIN_PREFIX=seaweedfs
DOMAIN=example.com

# Volume server domain
VOLUME_DOMAIN_PREFIX=seaweedfs-volume
# Filer server domain (file operations)
FILER_DOMAIN_PREFIX=seaweedfs-filer
# S3 API domain
S3_DOMAIN_PREFIX=seaweedfs-s3

# S3 API Credentials
S3_ACCESS_KEY= ## openssl rand -hex 10
S3_SECRET_KEY= ## openssl rand -hex 20

# Master Configuration
# Volume size limit in MB (default: 1000)
WEED_MASTER_VOLUME_SIZE_LIMIT_MB=1000

# CORS Configuration
# Comma-separated list of allowed origins (use * for all origins)
CORS_ALLOWED_ORIGINS=*

# Port Configuration (for localhost access)
# Uncomment these if you want localhost port access
# HOST_PORT_MASTER=9333
# HOST_PORT_VOLUME=8080
# HOST_PORT_FILER=8888
# HOST_PORT_S3=8333
